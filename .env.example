# Example environment variables — copy to .env and fill values
# Use PROVIDER=openai for OpenAI (gpt5/gpt-4o). Otherwise use watsonx.
PROVIDER=watsonx

# For WatsonX
WATSONX_BASE_URL=https://your-watsonx-endpoint.example
WATSONX_API_KEY=your_watsonx_api_key
WATSONX_PROJECT_ID=your_project_id
MODEL=granite-1
# Optional: path template for inference. Use placeholders {project_id} and {model} if needed.
# Example: /v1/projects/{project_id}/models/{model}/infer
INFER_PATH=/v1/generate

# For OpenAI
#OPENAI_API_KEY=sk-...
#MODEL=gpt-5
